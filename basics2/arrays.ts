const porsches: string[] = []

const horsepower: Array<number> =[] //another way of declaring a array 

type User = {
    name: string
    isVerified: boolean
}

const allUser: User[] = []//for a array of user defined type

const models: number[][] = [
    [201, 521, 632],
    [841, 852, 741],
    []
]//for nested arrays


porsches.push("918 spyder")
horsepower.push(750)

allUser.push({name: "", isVerified: false})